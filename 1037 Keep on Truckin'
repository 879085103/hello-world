// 1040.cpp: 定义控制台应用程序的入口点。
//

#include "stdafx.h"
#include<iostream>
#include<algorithm>

using namespace std;

int main()
{
	int a[5],i;
	int flag = 0;
	for ( i = 0; i < 3; i++)
	{
		cin >> a[i];
		if (a[i] <= 168)
		{
			flag = 1;
			break;
		}
	}
	if (flag == 1)
	{
		cout << "CRASH " << a[i] << endl;
	}
	else
	{
		cout<<"NO CRASH"<<endl;
	}
    return 0;
}

